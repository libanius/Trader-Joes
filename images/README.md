# Imagens do deck

Você tem dois fluxos:

## 1) Fluxo rápido (imagens sem nome)
1. Coloque as imagens em `images/inbox/` (qualquer nome).
2. Rode:
   ```bash
   python scripts/prepare_images.py


python scripts/prepare_images.py --strict
